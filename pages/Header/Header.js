import React from 'react';
import MainScreen from '@/components/MainScreen/MainScreen';

function Header() {
   return (
      <section id="header">
         <MainScreen />
      </section>
   );
}

export default Header;
